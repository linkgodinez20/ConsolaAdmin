@model IEnumerable<Security.Core.Model.LogIn>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Usuario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Senha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Salt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UsoSalt)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Personas.APaterno)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Usuario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Senha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Salt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UsoSalt)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Personas.APaterno)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Login }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Login }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Login })
        </td>
    </tr>
}

</table>
